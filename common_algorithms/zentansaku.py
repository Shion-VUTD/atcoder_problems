
from collections import deque
#BFS
que = deque([])
while que:
    q = que.popleft()
    #qを探索済みにする
    for i in []: #qの隣接リスト
        #iが探索済みでないなら
            #iを探索済みにする
            #iをqueにいれる

#DFS
que = deque([])
while que:
    q = que.pop()
    #qを探索済みにする
    for i in []: #qの隣接リスト
        #iが探索済みでないなら
            #iを探索済みにする
            #iをqueにいれる
